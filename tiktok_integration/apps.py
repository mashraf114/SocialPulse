from django.apps import AppConfig

class TiktokIntegrationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'tiktok_integration'  # ✅ FULL path from your project root
